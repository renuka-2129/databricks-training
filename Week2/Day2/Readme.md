# SQL Window Functions & Advanced Queries Practice

Practiced advanced SQL analytical queries using aggregation, joins, ranking functions, and window functions on a College Management System database.

## Topics Covered
- Aggregate Functions
- `GROUP BY` and `HAVING`
- `ORDER BY` and `DISTINCT`
- Window Functions
- Join Operations
- Running Totals and Ranking
- Department and Student Analysis

## Files Included
- `schema.sql`
- `queries.sql`
- `output.txt`
- `README.md`

## Key Learnings
- Used aggregate functions like `COUNT()`, `SUM()`, and `AVG()`
- Performed ranking using `ROW_NUMBER()`, `RANK()`, and `DENSE_RANK()`
- Calculated running totals using `OVER()`
- Worked with joins for relational data analysis
- Performed department-wise salary and student analysis
- Handled NULL values using `IS NULL` and `LEFT JOIN`

## Additional Practice
- Student performance tracking
- Top-N analysis queries
- Salary aggregation and ranking
- City-wise and department-wise reports
- Multi-table analytical queries

Successfully completed advanced SQL practice covering analytical queries, ranking functions, aggregation, joins, and window functions, improving overall database querying and problem-solving skills.
